{# macro para algunos div de los mensajes boxs#}
{% macro macro_delete_div(title_eliminar,texto_eliminar) %}
    <div style="display: none;z-index: 1000">
        <div id="dialog-confirm-delete" title="{{ title_eliminar | default('Eliminar') }}">
            <p><span class="ui-icon ui-icon-alert"
                     style="float:left; margin:0 7px 20px 0;"></span>{{ texto_eliminar | default('¿Deseas eliminar este registro?') }}
            </p>
        </div>
    </div>
{% endmacro %}