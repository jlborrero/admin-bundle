{% set pager_data = pager.paginationData %}
<div class="pagination">
    {% if pager_data.current == pager_data.first %}
        <a href="#" class="glyphicon-fast-backward" id="glyphicon-fast-backward_{{ zona }}"></a>
        <script type="text/javascript">
            $().ready(function () {
                $('#glyphicon-fast-backward_{{ zona }}').click(function(e) {
                    e.preventDefault();
                });
            });
        </script>
    {% else %}
        <a href="#" class="glyphicon-fast-backward" id="glyphicon-fast-backward_{{ zona }}"></a>
        <script type="text/javascript">
            $().ready(function () {
                $('#glyphicon-fast-backward_{{ zona }}').click(function(e) {
                    e.preventDefault();
                    {{ jq_remote_function({
                        'update':   zona, 
                        'url':      url,
                        'method':   'GET',
                        'with':     "'page=1'",
                        'complete': jq_sprintf("jQuery('#indicador_%s').hide();", zona),
                        'loading':  jq_sprintf("jQuery('#indicador_%s').show();", zona),
                    }) }}
                });
            });
        </script>
    {% endif %}
        
    {% if pager_data.previous is defined %}
        <a class="glyphicon-step-backward" href="#" id="glyphicon-step-backward_{{ zona }}"></a>
        <script type="text/javascript">
            $().ready(function () {
                $('#glyphicon-step-backward_{{ zona }}').click(function(e) {
                    e.preventDefault();
                    {{ jq_remote_function({
                        'update':   zona, 
                        'url':      url,
                        'method':   'GET',
                        'with':     jq_sprintf("'page=%s'", pager_data.previous),
                        'complete': jq_sprintf("jQuery('#indicador_%s').hide();", zona),
                        'loading':  jq_sprintf("jQuery('#indicador_%s').show();", zona),
                    }) }}
                });
            });
        </script>
    {% else %}
        <a href="#" class="glyphicon-step-backward" id="glyphicon-step-backward_{{ zona }}"></a>
        <script type="text/javascript">
            $().ready(function () {
                $('#glyphicon-step-backward_{{ zona }}').click(function(e) {
                    e.preventDefault();
                });
            });
        </script>
    {% endif %}
        
    <div class="pager-indicator">
        <span class="page">{{ pager_data.current }}</span> / <span class="cant">{{ pager_data.pageCount }}</span>
    </div>
        
    <select onchange="window.location='{{ url_changemaxperpage }}?cant=' + this.value">
        {% if maxperpage is defined %}
            {% for item in maxperpage_list %}
            <option {% if pager_data.numItemsPerPage == item %} selected="selected" {% endif %}>{{ item }}</option>
            {% endfor %}
        {% else %}
            <option {% if pager_data.numItemsPerPage == 10 %} selected="selected" {% endif %}>10</option>
            <option {% if pager_data.numItemsPerPage == 20 %} selected="selected" {% endif %}>20</option>
            <option {% if pager_data.numItemsPerPage == 50 %} selected="selected" {% endif %}>50</option>
        {% endif %}
    </select>
        
    {% if pager_data.next is defined %}
        <a class="glyphicon-step-forward" href="#" id="glyphicon-step-forward_{{ zona }}"></a>
        <script type="text/javascript">
            $().ready(function () {
                $('#glyphicon-step-forward_{{ zona }}').click(function(e) {
                    e.preventDefault();
                    {{ jq_remote_function({
                        'update':   zona, 
                         'url':      url,
                         'method':   'GET',
                         'with':     jq_sprintf("'page=%s'", pager_data.next),
                         'complete': jq_sprintf("jQuery('#indicador_%s').hide();", zona),
                         'loading':  jq_sprintf("jQuery('#indicador_%s').show();", zona),
                    }) }}
                });
            });
        </script>
    {% else %}
        <a href="#" class="glyphicon-step-forward" id="glyphicon-step-forward_{{ zona }}"></a>
        <script type="text/javascript">
            $().ready(function () {
                $('#glyphicon-step-forward_{{ zona }}').click(function(e) {
                    e.preventDefault();
                });
            });
        </script>
    {% endif %}
        
    {% if pager_data.current != pager_data.pageCount %}
        <a class="glyphicon-fast-forward" href="#" id="glyphicon-fast-forward_{{ zona }}"></a>
        <script type="text/javascript">
            $().ready(function () {
                $('#glyphicon-fast-forward_{{ zona }}').click(function(e) {
                    e.preventDefault();
                    {{ jq_remote_function({
                        'update':   zona, 
                        'url':      url,
                        'method':   'GET',
                        'with':     jq_sprintf("'page=%s'", pager_data.pageCount),
                        'complete': jq_sprintf("jQuery('#indicador_%s').hide();", zona),
                        'loading':  jq_sprintf("jQuery('#indicador_%s').show();", zona),
                    }) }}
                });
            });
        </script>
    {% else %}
        <a href="#" class="glyphicon-fast-forward" id="glyphicon-fast-forward_{{ zona }}"></a>
        <script type="text/javascript">
            $().ready(function () {
                $('#glyphicon-fast-forward_{{ zona }}').click(function(e) {
                    e.preventDefault();
                });
            });
        </script>
    {% endif %}
     
    <div class="loader">
        <img style="display: none;" src="{{ asset('bundles/jlbsadmin/images/loader.gif') }}" id="indicador_{{ zona }}"/>
    </div>
</div>