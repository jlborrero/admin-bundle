<table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered" id="show_{{ entity }}">
    <tbody>
    {%- for field, metadata in fields %}
        <tr>
            <th>{{ field|capitalize }}</th>

            {%- if metadata.type in ['date', 'datetime', 'datetimetz'] %}

                <td>{{ '{{ entity.' ~ field|replace({'_': ''}) ~ '|date(\'d/m/Y h:m:s\') }}' }}</td>

            {%- else %}

                <td>{{ '{{ entity.' ~ field|replace({'_': ''}) ~ ' }}' }}</td>

            {%- endif %}

        </tr>
    {%- endfor %}
    </tbody>
</table>