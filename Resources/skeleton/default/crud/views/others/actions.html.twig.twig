<div class="table_actions">
    {%- for action in record_actions %}
        {%- if  action == 'show' %}
            <a href="#"
               onclick="showDialog_{{ route_name_prefix }}('{{ "{{ path('" ~ route_name_prefix ~ "_" ~ action ~ "', { 'id': entity.id }) }}" }}')"
               class="btn btn-xs btn-primary" title="Mostrar"><i class="glyphglyphicon-eye-open glyphglyphicon-white"></i></a>
        {%- endif %}
        {%- if  action == 'new' %}
            <a href="{{ "{{ path('" ~ route_name_prefix ~ "_" ~ action ~ "', { 'id': entity.id }) }}" }}"
               class="btn btn-xs btn-success" title="Editar"><i
                        class="glyphglyphicon-edit glyphglyphicon-white"></i></a>
        {%- endif %}
        {%- if  action == 'delete' %}
            <a href="#"
               onclick="confirm_delete('{{ "{{ path('" ~ route_name_prefix ~ "_" ~ action ~ "', { 'id': entity.id }) }}" }}')"
               class="btn btn-xs btn-danger" title="Eliminar"><i class="glyphglyphicon-trash glyphglyphicon-white"></i></a>
        {%- endif %}
    {%- endfor %}
</div>